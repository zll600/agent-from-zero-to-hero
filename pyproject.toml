[project]
name = "agent-from-zero-to-hero"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
readme = "README.md"
dependencies = [
    "anthropic>=0.86.0",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
